# Product test matrix

| Flow | Status | Setup | Action | Expected | Actual | Evidence | Restart | Recovery |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Packaged app / Gatekeeper | passing | Published universal `v0.1.0-rc.1` candidate from the tagged source | Verify signatures, staple, Gatekeeper assessment, DMG, archive, and launch | Apple accepts the app and DMG; macOS launches the stapled Developer ID build | Both Apple submissions were accepted and every packaged verification passed | [Signed release candidate](evidence/v0.1.0/release-candidate.md) | The packaged app quit and relaunched successfully | Archive extraction and DMG verification provide independent recovery checks |
| Keychain signed-app integration | passing | Signed packaged app with disposable capability-specific Keychain items | Run set, get, delete, malformed-item, restart, and account-separation phases | Credentials remain outside SQLite and capabilities stay isolated across restart | Both signed-binary phases passed; malformed data failed closed without exposing material | [Native acceptance](evidence/v0.1.0/native-acceptance.md) | The second signed-binary phase passed after a real app restart | Retry restored a healthy connection after the malformed-item recovery branch |
| OAuth Device Flow / restart | passing | Bundled public client ID with separate PR-read and PR-publish capabilities | Complete each app-owned Device Flow grant and relaunch the signed app | Validated GitHub account survives restart without tokens in app data | Both capability-specific grants connected as `uhvesta`; the signed app recovered them independently after restart without exposing tokens | [Native acceptance](evidence/v0.1.0/native-acceptance.md) and [persisted connection screenshot](evidence/v0.1.0/oauth-keychain-persisted.jpeg) | PR-read and PR-publish remained connected after a complete signed-app restart | Expired pending codes were removed cleanly and the UI offered a fresh connection; capability items remained isolated |
| Local multi-repository commit-on-submit | passing | Two clean fixture repositories with staged review changes | Submit one packaged-UI round with the canonical brief and exact commit body | Both repositories commit atomically and preserve one immutable manifest | Both commits and the immutable round were created; both repositories remained clean | [Native acceptance](evidence/v0.1.0/native-acceptance.md) | The stored round reopened after packaged-app restart | Atomic rollback integration tests restore exact pre-submit repository state |
| Connected-machine immutable review | passing | Owner-only local Unix socket serving one remote review fixture | Connect, refresh metadata, open the full snapshot, preview, and reproduce | Remote reads are explicit; cached snapshot and detached reproduction match exact commits | One item was cached and reproduced cleanly at the recorded head without changing its source | [Native acceptance](evidence/v0.1.0/native-acceptance.md) and [screenshot](evidence/v0.1.0/connected-machine-reproduced.jpeg) | Cached immutable data remained available independently of the stopped daemon | Preview created nothing; confirmed materialization used a new clean destination |
| Connected-machine SSH tunnel | passing with limitation | Shipped standalone daemon, disposable Git source, ephemeral agent and unprivileged SSH server, isolated system-OpenSSH host alias | Start an actual `/usr/bin/ssh -N -L` Unix-socket forward; fetch health, lazy index, detail, and immutable snapshot | The system SSH path reaches the token-free daemon without storing key, passphrase, agent path, or mutable source data | The real tunnel passed inside the signed-candidate pipeline; desktop-form config excluded private-key bytes and key/agent paths, and the remote HEAD plus full status remained unchanged. Native machine UI was already proven with the identical local-socket protocol; the host alias was exercised by the production SSH argument contract rather than retained user SSH configuration. | [Real SSH tunnel record](evidence/v0.1.0/real-ssh-tunnel.md) | A fresh agent/server/daemon process was created for each deterministic run; no cached credential state was reused | Tunnel startup failure preserves cached rounds and exposes inspect-target, reconnect, disconnect, and delete recovery |
| Copilot streaming / cancel / no-replay | passing | Developer-ID-signed app using the official SDK and existing Copilot CLI sign-in | Reset a disclosed stale saved option with zero prompts, send one anchored prompt, cancel a streaming follow-up, restart, and clear chat | Only explicit prompts are sent; stale options are never passed silently; cancelled content is not replayed or resumed | The stale `context_window=managed_80` value was disclosed and removed only after an explicit zero-turn reset; the new anchored turn completed once, and completed/cancelled history persisted without replay | [Native acceptance](evidence/v0.1.0/native-acceptance.md), [stale disclosure](evidence/v0.1.0/copilot-stale-option-disclosure.jpeg), and [restart evidence](evidence/v0.1.0/copilot-stale-option-restart.jpeg) | Restart retained exactly one new completed anchored turn, made the provider session history-only, and created no additional turn | Clear chat is the only recovery for the active restart-lost transcript; archived and completed chats cannot start, send, clear, or retry |
| GitHub mirror / imported discussion | passing | Disposable non-self-authored pull request and validated PR-read connection | Intake metadata, lazily fetch the immutable snapshot, and inspect imported discussion | Metadata remains lightweight until open and imported threads retain provenance | Round `6e7fb133...` cached the immutable `9d21e8bd...` head; the reviewer reopened under an unreachable proxy and rendered imported PR comment `5124939624`, inline comment `3679034978`, and review summary `4814144577` with provenance intact after fixing empty-root and lowercase-side rendering regressions | [Native acceptance](evidence/v0.1.0/native-acceptance.md) and [imported discussion screenshot](evidence/v0.1.0/github-imported-discussion.jpeg) | Restart reopened the cached immutable mirror without an implicit remote read | An unreachable proxy preserved the prior snapshot and kept the reviewer usable |
| GitHub exact publish | passing | Local approval with a separate validated PR-publish connection | Confirm publication once, retry, and inspect the GitHub review receipt | Exactly one APPROVE review is created and retries are idempotent | One APPROVED review `4814281693` and reply `3679134377` were published; GitHub associated the reply with COMMENTED container `4814281740`, not a duplicate approval, and completed receipts retained unchanged IDs after restart | [Native acceptance](evidence/v0.1.0/native-acceptance.md) and [publish receipt screenshot](evidence/v0.1.0/github-publish-receipt.jpeg) | Restart retained both completed publication receipts with unchanged upstream IDs and no duplicate review | Retry reconciles the same completed receipts and upstream IDs |
| Manual originating-agent handoff | passing | A changes-requested round with one immutable line-anchored comment | Prepare formal feedback, reproduce the snapshot, submit to a fresh agent, and acknowledge manually | No prompt is injected into a running session; the exact snapshot and prompt are handed off | Fresh agent verified both commits and manual submission was recorded in history | [Native acceptance](evidence/v0.1.0/native-acceptance.md) and [screenshot](evidence/v0.1.0/manual-handoff-confirmed.jpeg) | Historical pending delivery exposed the manual acknowledgement after remount | Reproduction commands create a clean detached environment without touching sources |
| ACP exact delivery / revisions | passing | Notarized universal candidate, disposable product database, registered loopback ACP route, and fake agent with persisted dedupe state | Confirm revision 1, drop its ACK, restart, explicitly retry the same key, edit the delivered comment, then confirm a new revision-2-only delivery | The first immutable key is accepted once despite retry; the edit creates a new key carrying only revision 2; prepare/open/restart/cancel never send | The production desktop backend performed one phase-one attempt, reused the first key after process restart with duplicate acceptance suppressed, then delivered only revision 2 under a new key. UI tests separately proved exact-target confirmation, cancel-with-zero-call, Queue/Interrupt policy, and copy/reproduction recovery. | [Packaged ACP JSONL](evidence/v0.1.0/packaged-acp-delivery.jsonl) and [native acceptance](evidence/v0.1.0/native-acceptance.md) | Phase two reopened the same disposable SQLite database in a separate production executable process and issued no request until explicit retry | Dropped/invalid ACK retains the same immutable key and undelivered revisions; unreachable/rejected routes preserve Copy and Reproduce recovery |
| Packaged lifecycle / purge / resubmit | passing | Notarized universal candidate with two disposable Git repositories and a disposable SQLite database | Submit/resubmit, Request changes, Complete, Requeue, reorder, cancel and confirm Delete/local Approve, then reopen in a separate process | Only explicit lifecycle events occur; cancel preserves state; confirmed terminal actions purge app-owned round state; ranks/supersession survive restart; sources remain untouched | Both production executable phases passed. The state log contained the expected transitions, canceled confirmations returned `confirmation_required`, confirmed Delete/Approve purged, ranks were identical after restart, superseded history remained, and repository HEAD/tree/status were unchanged. | [Packaged lifecycle JSONL](evidence/v0.1.0/packaged-lifecycle.jsonl) and [native acceptance](evidence/v0.1.0/native-acceptance.md) | Phase two reopened the same SQLite database and revalidated lifecycle, ranks, supersession, and sources before terminal actions | Cancel performs no API call; source commits/files are outside app-owned purge and remain the reproduction recovery |
| CLI ingestion parity | passing | Real token-free CLI binary, owner-only fixture socket, and canonical UI/core records | Run `pr add`, `machine add`, and `submit` twice with `--json`, including secret-shaped environment values | Reruns succeed with the existing ID and exact JSON echo; CLI/UI records serialize identically; no credential reaches the socket; delivery/publish remain forbidden | Four real-binary parity integrations plus core/UI comparisons passed. The socket received no secret-shaped value and rejected both delivery and publish request types. | [Rev3 signed acceptance candidate](evidence/v0.1.0/rev3-acceptance-candidate.md) and [native acceptance](evidence/v0.1.0/native-acceptance.md) | Each rerun used a fresh CLI process and retained the original record identity | Unreachable app, missing capability, and invalid machine configuration return distinct exits with the exact launch/connect/correct-field action |
| Error and recovery contract | passing | Shared schema-v3 catalog with all 18 §8 states and 75 unique backend-code mappings | Snapshot every catalog entry in UI/CLI JSON, drive real CLI unreachable/capability/config failures, and inspect publish-without-decision | Every state has non-generic what/why/data-safety/one-next-action/diagnostics/back copy; generic or incomplete copy fails; prevented publish is disabled with reason | Catalog completeness/uniqueness tests, CLI JSON snapshots, rendered UI contract assertions, real exit-code integrations, and the unreachable Publish review control all passed | [Rev3 signed acceptance candidate](evidence/v0.1.0/rev3-acceptance-candidate.md) and [native acceptance](evidence/v0.1.0/native-acceptance.md) | Catalog-backed copy is deterministic after process restart and carries no volatile credential state | Each state exposes one exact recovery action plus a cancel/back route; diagnostics remain redacted |
| difit-derived reviewer / responsive panes | passing | Dev-only fixture review with nested paths, a durable interrupted `/ask` turn, and 1280/1024/560 viewports | Exercise repository-tree filtering/expansion, continuous multi-file diffs, Files/Chat controls, mode state, viewed progress, durable-chat retry, archived/completed chat read-only enforcement, passive reviewer open, refreshed-round isolation, cached-machine rematerialization, keyboard diff navigation, local version visibility, ACP confirmation, lifecycle dialogs, and recovery-copy states | All fixture tests pass; every responsive escape hatch remains reachable; retained packaged-app evidence verifies the same behavior | Frontend and fixture builds pass; 42/42 frontend tests pass. Browser checks at 1280/1024/560 and a universal packaged-app run at the 560px minimum confirmed non-zero diff layout, Files/Chat controls, continuous files, and explicit interrupted-turn retry with retained history. Focused regressions additionally confirm ACP exact-target confirmation and busy recovery, both purge dialogs, all 18 recovery states, scoped reorder shortcuts, and cached GitHub discussion without an implicit refresh. | [difit migration report](reference-ui/difit-migration-report.md) and [native acceptance evidence](evidence/v0.1.0/native-acceptance.md) | Packaged app reopened the persisted multi-repository round; restart-separated ACP/lifecycle phases revalidated durable behavior | Keychain denial preserves credentials with actionable recovery; ACP and lifecycle Cancel paths preserve all local work |
| Signed updater / relaunch | blocked | Installed signed `v0.1.0-rc.1` candidate and signed stable-update configuration | Publish stable, check, confirm install, then separately confirm relaunch | Candidate discovers exact `0.1.0`, verifies its signature, installs, and relaunches with data intact | Candidate artifacts and verifier tests pass; live candidate-to-stable flow awaits stable publication | [Signed release candidate](evidence/v0.1.0/release-candidate.md) | Relaunch is a separate explicit confirmation covered by deterministic tests | A failed check or install leaves the candidate and review data unchanged |
| Token-leak artifact scan | passing | Complete signed acceptance directory and live application-support directory | Mount/extract and scan the app, DMG, ZIP, updater archive, manifests, SQLite, and regular runtime files without printing matches | No credential patterns occur in shipped or persisted artifacts; compressed bytes cannot create false credential reports | The payload-aware hardened scan passed the current notarized candidate and all seven checksummed artifacts; clear/leaking app-archive and DMG fixtures prove fail-closed behavior without echoing matches | [Token-leak scan](evidence/v0.1.0/token-leak-scan.md) and [Rev3 candidate](evidence/v0.1.0/rev3-acceptance-candidate.md) | The scan covered live data created across signed-app restarts and both disposable packaged acceptance phases | The scanner reports only the affected path, unmounts/removes temporary extraction state, and never prints matched material |

Rows move to `passing` or `passing with limitation` only after the retained
evidence demonstrates the exact native flow. Stable publication is gated by
`scripts/check-release-readiness.sh`.
